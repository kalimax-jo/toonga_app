import 'package:flutter/material.dart';

class AppColors {
  // Toonga Primary Brand Color (Yellow)
  static const Color primary = Color(0xFFF0B739);

  // Background Colors
  static const Color background = Color(0xFF000000); // Dark black
  static const Color card = Color(0xFF111111); // Slightly lighter black

  // Text Colors
  static const Color textLight = Colors.white;
  static const Color textDark = Colors.black;
  static const Color textGrey = Colors.white70;

  // Accent Colors
  static const Color accent = Color(0xFFf0b739);

  // Error / Success
  static const Color success = Color(0xFF1DB954);
  static const Color error = Color(0xFFE53935);
}
